#include <string>
#include "PhoneBook.hpp"

int	main(void)
{
	PhoneBook MyPhoneBook;
	std::string input = "";

	if (input != "EXIT")
	{
		if (input == "ADD")
		{

		}
		else if (input == "SEARCH")
		{

		}
		std::cin >> input;
	}

	return (0);
}
